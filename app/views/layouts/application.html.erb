<!DOCTYPE html>
<html lang="th" class="h-full">
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "UniMatch - จับคู่เพื่อนติว" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
    <meta name="description" content="UniMatch — แพลตฟอร์มจับคู่เพื่อนติวสำหรับนักศึกษา ค้นหาบัดดี้ที่เหมาะกับสไตล์การเรียนของคุณ">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="UniMatch">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#0ea5e9">

    <!-- Open Graph -->
    <meta property="og:title" content="UniMatch - จับคู่เพื่อนติว">
    <meta property="og:description" content="แพลตฟอร์มจับคู่เพื่อนติวสำหรับนักศึกษา พร้อม AI ติวเตอร์">
    <meta property="og:type" content="website">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Kanit Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <meta name="turbo-cache-control" content="no-preview">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      * { font-family: 'Kanit', sans-serif; }
    </style>
  </head>

  <body class="h-full bg-gray-100 text-slate-800">
    <% if logged_in? %>
      <!-- ===== DESKTOP NAVIGATION ===== -->
      <nav class="bg-white border-b border-gray-100 sticky top-0 z-50 shadow-sm hidden md:block">
        <div class="max-w-6xl mx-auto px-6">
          <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <%= link_to dashboard_path, class: "flex items-center gap-2.5 group" do %>
              <div class="w-9 h-9 bg-sky-500 rounded-xl flex items-center justify-center shadow-sm">
                <i class="fa-solid fa-graduation-cap text-white text-sm"></i>
              </div>
              <span class="font-bold text-xl text-slate-800 group-hover:text-sky-500 transition-colors">UniMatch</span>
            <% end %>

            <!-- Nav Links -->
            <div class="flex items-center gap-1">
              <%= link_to dashboard_path, class: "flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-medium transition-all duration-200 #{request.path == dashboard_path ? 'bg-sky-50 text-sky-600' : 'text-slate-500 hover:text-sky-500 hover:bg-gray-50'}" do %>
                <i class="fa-solid fa-compass"></i>
                ค้นหา
              <% end %>
              <%= link_to chat_rooms_path, class: "flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-medium transition-all duration-200 #{request.path == chat_rooms_path ? 'bg-sky-50 text-sky-600' : 'text-slate-500 hover:text-sky-500 hover:bg-gray-50'}" do %>
                <i class="fa-solid fa-comment"></i>
                แชท
              <% end %>
              <%= link_to edit_profile_path, class: "flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-medium transition-all duration-200 #{request.path == edit_profile_path ? 'bg-sky-50 text-sky-600' : 'text-slate-500 hover:text-sky-500 hover:bg-gray-50'}" do %>
                <i class="fa-solid fa-user"></i>
                โปรไฟล์
              <% end %>
            </div>

            <!-- Profile -->
            <div class="flex items-center gap-3">
              <%= link_to edit_profile_path, class: "flex items-center gap-2.5 group" do %>
                <div class="w-9 h-9 rounded-full overflow-hidden ring-2 ring-sky-100">
                  <img src="https://api.dicebear.com/9.x/adventurer/svg?seed=<%= current_user.name %>&backgroundColor=b6e3f4" alt="avatar" class="w-full h-full object-cover">
                </div>
                <span class="text-sm font-medium text-slate-600 group-hover:text-sky-500 transition-colors"><%= current_user.name %></span>
              <% end %>
              <%= button_to logout_path, method: :delete, class: "text-slate-400 hover:text-rose-500 transition-colors ml-1 p-2 rounded-lg hover:bg-rose-50", data: { turbo_method: :delete } do %>
                <i class="fa-solid fa-right-from-bracket"></i>
              <% end %>
            </div>
          </div>
        </div>
      </nav>

      <!-- ===== MOBILE TOP BAR ===== -->
      <nav class="bg-sky-500 sticky top-0 z-50 shadow-md md:hidden relative overflow-hidden">
        <div class="absolute -top-10 -right-10 w-24 h-24 bg-white opacity-10 rounded-full blur-2xl"></div>
        <div class="px-4 flex justify-between items-center h-14 relative z-10">
          <%= link_to dashboard_path, class: "flex items-center gap-2" do %>
            <i class="fa-solid fa-graduation-cap text-white"></i>
            <span class="font-bold text-lg text-white">UniMatch</span>
          <% end %>
          <div class="flex items-center gap-2">
            <p class="text-sky-100 text-[10px] font-light hidden sm:block">หาเพื่อนติวที่ใช่</p>
            <%= link_to edit_profile_path do %>
              <div class="bg-white/20 w-8 h-8 flex items-center justify-center rounded-full backdrop-blur-sm hover:bg-white/30 transition">
                <i class="fa-solid fa-user text-white text-xs"></i>
              </div>
            <% end %>
          </div>
        </div>
      </nav>

      <!-- ===== BOTTOM NAVIGATION (Mobile) ===== -->
      <nav class="fixed bottom-0 left-0 right-0 z-50 bg-white border-t border-gray-100 shadow-[0_-4px_6px_-1px_rgba(0,0,0,0.05)] md:hidden safe-area-bottom">
        <div class="flex justify-around items-center h-16 px-2">
          <%= link_to dashboard_path, class: "flex flex-col items-center gap-0.5 py-1.5 px-3 rounded-xl transition-all #{request.path == dashboard_path ? 'text-sky-500' : 'text-slate-400'}" do %>
            <i class="fa-solid fa-compass text-lg"></i>
            <span class="text-[10px] font-medium">ค้นหา</span>
          <% end %>
          <%= link_to chat_rooms_path, class: "flex flex-col items-center gap-0.5 py-1.5 px-3 rounded-xl transition-all #{request.path == chat_rooms_path ? 'text-sky-500' : 'text-slate-400'}" do %>
            <i class="fa-solid fa-comment text-lg"></i>
            <span class="text-[10px] font-medium">แชท</span>
          <% end %>
          <%= link_to edit_profile_path, class: "flex flex-col items-center gap-0.5 py-1.5 px-3 rounded-xl transition-all #{request.path == edit_profile_path ? 'text-sky-500' : 'text-slate-400'}" do %>
            <i class="fa-solid fa-user text-lg"></i>
            <span class="text-[10px] font-medium">โปรไฟล์</span>
          <% end %>
          <%= button_to logout_path, method: :delete, class: "flex flex-col items-center gap-0.5 py-1.5 px-3 rounded-xl transition-all text-slate-400 hover:text-rose-500", data: { turbo_method: :delete, turbo_confirm: "ต้องการออกจากระบบ?" } do %>
            <i class="fa-solid fa-right-from-bracket text-lg"></i>
            <span class="text-[10px] font-medium">ออก</span>
          <% end %>
        </div>
      </nav>
    <% end %>

    <!-- ===== FLASH MESSAGES ===== -->
    <% if flash[:notice] %>
      <div class="max-w-lg md:max-w-2xl lg:max-w-4xl mx-auto px-4 md:px-6 mt-4 animate-slide-up">
        <div class="bg-emerald-50 border border-emerald-200 text-emerald-700 px-4 py-3 rounded-xl flex items-center gap-3 shadow-sm">
          <i class="fa-solid fa-circle-check flex-shrink-0"></i>
          <span class="text-sm font-medium"><%= flash[:notice] %></span>
        </div>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="max-w-lg md:max-w-2xl lg:max-w-4xl mx-auto px-4 md:px-6 mt-4 animate-slide-up">
        <div class="bg-rose-50 border border-rose-200 text-rose-700 px-4 py-3 rounded-xl flex items-center gap-3 shadow-sm">
          <i class="fa-solid fa-circle-exclamation flex-shrink-0"></i>
          <span class="text-sm font-medium"><%= flash[:alert] %></span>
        </div>
      </div>
    <% end %>

    <!-- ===== MAIN CONTENT ===== -->
    <main class="<%= 'pb-20 md:pb-6' if logged_in? %>">
      <%= yield %>
    </main>
  </body>
</html>
